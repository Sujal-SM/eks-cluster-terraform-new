# Local .terraform directories
**/.terraform/*

# Terraform state files
*.tfstate
*.tfstate.*
crash.log

# Exclude all .tfvars files, which typically contain sensitive data
*.tfvars
*.tfvars.json

# Exclude override files as they are usually local-only
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Exclude CLI configuration files
.terraformrc
terraform.rc

# Lock file (optional: keep if using consistent provider versions)
.terraform.lock.hcl
